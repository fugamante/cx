diff --git a/common/arg.cpp b/common/arg.cpp
index 649216b..16802ca 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -2020,6 +2020,27 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             params.cache_type_v = kv_cache_type_from_str(value);
         }
     ).set_env("LLAMA_ARG_CACHE_TYPE_V"));
+    add_opt(common_arg(
+        {"--turboquant-enable"},
+        "enable experimental TurboQuant-style V-cache sidecar path",
+        [](common_params & params) {
+            params.turboquant_enable = true;
+        }
+    ).set_env("LLAMA_ARG_TURBOQUANT_ENABLE"));
+    add_opt(common_arg(
+        {"--turboquant-group-size"}, "N",
+        string_format("experimental TurboQuant V-cache group size (default: %u)", params.turboquant_group_size),
+        [](common_params & params, int value) {
+            params.turboquant_group_size = value > 0 ? (uint32_t) value : params.turboquant_group_size;
+        }
+    ).set_env("LLAMA_ARG_TURBOQUANT_GROUP_SIZE"));
+    add_opt(common_arg(
+        {"--turboquant-codebook-bits"}, "N",
+        string_format("experimental TurboQuant V-cache codebook bits (default: %u)", params.turboquant_codebook_bits),
+        [](common_params & params, int value) {
+            params.turboquant_codebook_bits = value > 0 ? (uint32_t) value : params.turboquant_codebook_bits;
+        }
+    ).set_env("LLAMA_ARG_TURBOQUANT_CODEBOOK_BITS"));
     add_opt(common_arg(
         {"--hellaswag"},
         "compute HellaSwag score over random tasks from datafile supplied with -f",
diff --git a/common/common.cpp b/common/common.cpp
index 16f78de..e31625b 100644
--- a/common/common.cpp
+++ b/common/common.cpp
@@ -1479,6 +1479,9 @@ struct llama_context_params common_context_params_to_llama(const common_params &
 
     cparams.type_k = params.cache_type_k;
     cparams.type_v = params.cache_type_v;
+    cparams.turboquant_enable = params.turboquant_enable;
+    cparams.turboquant_group_size = params.turboquant_group_size;
+    cparams.turboquant_codebook_bits = params.turboquant_codebook_bits;
 
     return cparams;
 }
diff --git a/common/common.h b/common/common.h
index 31a337d..29d3b19 100644
--- a/common/common.h
+++ b/common/common.h
@@ -548,6 +548,9 @@ struct common_params {
 
     ggml_type cache_type_k = GGML_TYPE_F16; // KV cache data type for the K
     ggml_type cache_type_v = GGML_TYPE_F16; // KV cache data type for the V
+    bool turboquant_enable = false;         // experimental V-cache sidecar quantization
+    uint32_t turboquant_group_size = 64;    // experimental V-cache group size
+    uint32_t turboquant_codebook_bits = 8;  // experimental V-cache codebook width
 
     common_conversation_mode conversation_mode = COMMON_CONVERSATION_MODE_AUTO;
 
diff --git a/include/llama.h b/include/llama.h
index a940f9d..63f2cbc 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -354,6 +354,9 @@ extern "C" {
 
         enum ggml_type type_k; // data type for K cache [EXPERIMENTAL]
         enum ggml_type type_v; // data type for V cache [EXPERIMENTAL]
+        bool          turboquant_enable;        // experimental V-cache sidecar quantization toggle
+        uint32_t      turboquant_group_size;    // experimental V-cache group size
+        uint32_t      turboquant_codebook_bits; // experimental V-cache codebook width
 
         // Abort callback
         // if it returns true, execution of llama_decode() will be aborted
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index a808e3e..ffb9957 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -273,9 +273,12 @@ llama_context::llama_context(
     // init the memory module
     if (!hparams.vocab_only) {
         llama_memory_params params_mem = {
-            /*.type_k   =*/ params.type_k,
-            /*.type_v   =*/ params.type_v,
-            /*.swa_full =*/ params.swa_full,
+            /*.type_k                 =*/ params.type_k,
+            /*.type_v                 =*/ params.type_v,
+            /*.turboquant_enable      =*/ params.turboquant_enable,
+            /*.turboquant_group_size  =*/ params.turboquant_group_size,
+            /*.turboquant_codebook_bits =*/ params.turboquant_codebook_bits,
+            /*.swa_full               =*/ params.swa_full,
         };
 
         memory.reset(model.create_memory(params_mem, cparams));
@@ -2904,6 +2907,9 @@ llama_context_params llama_context_default_params() {
         /*.cb_eval_user_data           =*/ nullptr,
         /*.type_k                      =*/ GGML_TYPE_F16,
         /*.type_v                      =*/ GGML_TYPE_F16,
+        /*.turboquant_enable           =*/ false,
+        /*.turboquant_group_size       =*/ 64,
+        /*.turboquant_codebook_bits    =*/ 8,
         /*.abort_callback              =*/ nullptr,
         /*.abort_callback_data         =*/ nullptr,
         /*.embeddings                  =*/ false,
diff --git a/src/llama-kv-cache-iswa.cpp b/src/llama-kv-cache-iswa.cpp
index 15b3fe1..84ffa44 100644
--- a/src/llama-kv-cache-iswa.cpp
+++ b/src/llama-kv-cache-iswa.cpp
@@ -60,7 +60,7 @@ llama_kv_cache_iswa::llama_kv_cache_iswa(
     LLAMA_LOG_INFO("%s: creating non-SWA KV cache, size = %u cells\n", __func__, size_base);
 
     kv_base = std::make_unique<llama_kv_cache>(
-            model, type_k, type_v,
+            model, type_k, type_v, false, 64, 8,
             v_trans, offload, unified, size_base, n_seq_max, n_pad,
             0, LLAMA_SWA_TYPE_NONE, filter_base, reuse);
 
@@ -68,7 +68,7 @@ llama_kv_cache_iswa::llama_kv_cache_iswa(
 
     // note: the SWA cache is never quantized because it is relatively small
     kv_swa = std::make_unique<llama_kv_cache>(
-            model, GGML_TYPE_F16, GGML_TYPE_F16,
+            model, GGML_TYPE_F16, GGML_TYPE_F16, false, 64, 8,
             v_trans, offload, unified, size_swa, n_seq_max, n_pad,
             hparams.n_swa, hparams.swa_type, filter_swa, reuse);
 }
diff --git a/src/llama-kv-cache.cpp b/src/llama-kv-cache.cpp
index 3e0fd31..5f61d59 100644
--- a/src/llama-kv-cache.cpp
+++ b/src/llama-kv-cache.cpp
@@ -80,6 +80,9 @@ llama_kv_cache::llama_kv_cache(
         const llama_model & model,
                 ggml_type   type_k,
                 ggml_type   type_v,
+                     bool   turboquant_enable,
+                 uint32_t   turboquant_group_size,
+                 uint32_t   turboquant_codebook_bits,
                      bool   v_trans,
                      bool   offload,
                      bool   unified,
@@ -92,6 +95,9 @@ llama_kv_cache::llama_kv_cache(
     const  layer_reuse_cb & reuse) :
     model(model), hparams(model.hparams), v_trans(v_trans),
     n_seq_max(n_seq_max), n_stream(unified ? 1 : n_seq_max), n_pad(n_pad), n_swa(n_swa), swa_type(swa_type) {
+    turboquant.enable = turboquant_enable;
+    turboquant.group_size = turboquant_group_size;
+    turboquant.codebook_bits = turboquant_codebook_bits;
 
     GGML_ASSERT(kv_size % n_pad == 0);
 
@@ -211,6 +217,7 @@ llama_kv_cache::llama_kv_cache(
         map_layer_ids[il] = layers.size();
 
         layers.push_back({ il, k, v, k_stream, v_stream, });
+        tq_v_layers.push_back({ il, 0, {} });
     }
 
     if (reuse) {
@@ -266,6 +273,10 @@ llama_kv_cache::llama_kv_cache(
                 (float)(memory_size_k + memory_size_v) / (1024.0f * 1024.0f), kv_size, (int) layers.size(), n_seq_max, n_stream,
                 ggml_type_name(type_k), (float)memory_size_k / (1024.0f * 1024.0f),
                 ggml_type_name(type_v), (float)memory_size_v / (1024.0f * 1024.0f));
+        if (turboquant.enable) {
+            LLAMA_LOG_INFO("%s: turboquant_v = 1, group_size = %u, codebook_bits = %u\n",
+                    __func__, turboquant.group_size, turboquant.codebook_bits);
+        }
     }
 
     const char * LLAMA_ATTN_ROT_DISABLE = getenv("LLAMA_ATTN_ROT_DISABLE");
@@ -315,6 +326,8 @@ llama_kv_cache::llama_kv_cache(
 }
 
 void llama_kv_cache::clear(bool data) {
+    turboquant_fallback = false;
+    turboquant_fallback_reason.clear();
     for (uint32_t s = 0; s < n_stream; ++s) {
         v_cells[s].reset();
         v_heads[s] = 0;
@@ -1113,6 +1126,45 @@ ggml_type llama_kv_cache::type_v() const {
     return layers[0].v->type;
 }
 
+bool llama_kv_cache::turboquant_supported_v(const ggml_tensor * v_cur) const {
+    if (!turboquant.enable || v_cur == nullptr) {
+        return false;
+    }
+
+    if (v_trans) {
+        return false;
+    }
+
+    if (turboquant.group_size == 0 || turboquant.codebook_bits == 0) {
+        return false;
+    }
+
+    if (v_cur->type != GGML_TYPE_F16 && v_cur->type != GGML_TYPE_F32) {
+        return false;
+    }
+
+    const int64_t n_embd_gqa = v_cur->ne[0]*v_cur->ne[1];
+    return n_embd_gqa % turboquant.group_size == 0;
+}
+
+void llama_kv_cache::turboquant_set_fallback(const char * reason) const {
+    if (!turboquant.enable) {
+        return;
+    }
+
+    const char * why = reason ? reason : "unknown";
+    if (!turboquant_fallback || turboquant_fallback_reason != why) {
+        LLAMA_LOG_WARN("%s: turboquant fallback active for V cache: %s\n", __func__, why);
+    }
+
+    turboquant_fallback = true;
+    turboquant_fallback_reason = why;
+}
+
+bool llama_kv_cache::turboquant_is_active_v() const {
+    return turboquant.enable && !turboquant_fallback;
+}
+
 uint32_t llama_kv_cache::get_n_kv(const slot_info & sinfo) const {
     uint32_t result = 0;
 
@@ -1150,6 +1202,10 @@ ggml_tensor * llama_kv_cache::get_k(ggml_context * ctx, int32_t il, uint32_t n_k
 }
 
 ggml_tensor * llama_kv_cache::get_v(ggml_context * ctx, int32_t il, uint32_t n_kv, const slot_info & sinfo) const {
+    if (turboquant.enable && !turboquant_fallback) {
+        turboquant_set_fallback("codec_not_implemented");
+    }
+
     const int32_t ikv = map_layer_ids.at(il);
 
     auto * v = layers[ikv].v;
@@ -1219,6 +1275,14 @@ ggml_tensor * llama_kv_cache::cpy_k(ggml_context * ctx, ggml_tensor * k_cur, ggm
 ggml_tensor * llama_kv_cache::cpy_v(ggml_context * ctx, ggml_tensor * v_cur, ggml_tensor * v_idxs, int32_t il, const slot_info & sinfo) const {
     GGML_UNUSED(sinfo);
 
+    if (turboquant.enable) {
+        if (!turboquant_supported_v(v_cur)) {
+            turboquant_set_fallback("unsupported_v_layout");
+        } else if (!turboquant_fallback) {
+            turboquant_set_fallback("codec_not_implemented");
+        }
+    }
+
     const int32_t ikv = map_layer_ids.at(il);
 
     auto * v = layers[ikv].v;
diff --git a/src/llama-kv-cache.h b/src/llama-kv-cache.h
index d4569a0..4916788 100644
--- a/src/llama-kv-cache.h
+++ b/src/llama-kv-cache.h
@@ -97,6 +97,9 @@ public:
             const llama_model & model,
                     ggml_type   type_k,
                     ggml_type   type_v,
+                         bool   turboquant_enable,
+                     uint32_t   turboquant_group_size,
+                     uint32_t   turboquant_codebook_bits,
                          bool   v_trans,
                          bool   offload,
                          bool   unified,
@@ -209,6 +212,25 @@ public:
     void set_input_v_rot(ggml_tensor * dst) const;
 
 private:
+    struct tq_v_params {
+        bool enable = false;
+        uint32_t group_size = 64;
+        uint32_t codebook_bits = 8;
+    };
+
+    struct tq_v_group {
+        uint32_t slot = 0;
+        uint32_t width = 0;
+        std::vector<uint8_t> codes;
+        std::vector<uint16_t> scales_fp16;
+    };
+
+    struct tq_v_layer_state {
+        uint32_t il = 0;
+        size_t raw_bytes = 0;
+        std::vector<tq_v_group> groups;
+    };
+
     const llama_model & model;
     const llama_hparams & hparams;
 
@@ -238,6 +260,9 @@ private:
     // env: LLAMA_ATTN_ROT_DISABLE
     bool attn_rot_k = false;
     bool attn_rot_v = false;
+    tq_v_params turboquant;
+    mutable bool turboquant_fallback = false;
+    mutable std::string turboquant_fallback_reason;
 
     // pre-computed hadamard martrices
     std::unordered_map<int64_t, std::vector<float>> attn_rot_hadamard;
@@ -267,6 +292,7 @@ private:
 
     // model layer id -> KV cache layer id
     std::unordered_map<int32_t, int32_t> map_layer_ids;
+    std::vector<tq_v_layer_state> tq_v_layers;
 
     size_t total_size() const;
 
@@ -299,6 +325,9 @@ private:
 
     bool state_read_meta(llama_io_read_i & io, uint32_t strm, uint32_t cell_count,       slot_info & sinfo, llama_seq_id dest_seq_id = -1);
     bool state_read_data(llama_io_read_i & io, uint32_t strm, uint32_t cell_count, const slot_info & sinfo);
+    bool turboquant_supported_v(const ggml_tensor * v_cur) const;
+    void turboquant_set_fallback(const char * reason) const;
+    bool turboquant_is_active_v() const;
 };
 
 class llama_kv_cache_context : public llama_memory_context_i {
diff --git a/src/llama-memory-hybrid.cpp b/src/llama-memory-hybrid.cpp
index 4ce1af5..3358172 100644
--- a/src/llama-memory-hybrid.cpp
+++ b/src/llama-memory-hybrid.cpp
@@ -34,6 +34,9 @@ llama_memory_hybrid::llama_memory_hybrid(
         model,
         type_k,
         type_v,
+        false,
+        64,
+        8,
         v_trans,
         offload,
         unified,
diff --git a/src/llama-memory.h b/src/llama-memory.h
index 4a157b9..166e964 100644
--- a/src/llama-memory.h
+++ b/src/llama-memory.h
@@ -18,6 +18,11 @@ struct llama_memory_params {
     ggml_type type_k;
     ggml_type type_v;
 
+    // optional TurboQuant prototype config
+    bool     turboquant_enable;
+    uint32_t turboquant_group_size;
+    uint32_t turboquant_codebook_bits;
+
     // use full-size SWA cache
     bool swa_full;
 };
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index ba93534..c2a6f5b 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -8386,6 +8386,9 @@ llama_memory_i * llama_model::create_memory(const llama_memory_params & params,
                                 *this,
                                 params.type_k,
                                 params.type_v,
+                                params.turboquant_enable,
+                                params.turboquant_group_size,
+                                params.turboquant_codebook_bits,
                                 !cparams.flash_attn,
                                 cparams.offload_kqv,
                                 cparams.kv_unified,
